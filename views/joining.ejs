<div v-if="player_state.admin">
    <button class="btn btn-primary" @click="next_page()">Start game</button>
</div>
<div v-if="spectator">
    <p>You are a spectator! Please reload if you want to play.</p>
</div>
<strong>Waiting for the admin to start!</strong>
<ul id="players">
    <p>Players:</p>
    <li v-for="(item, key) in other_player_state">{{key}}</li>
</ul>